import React from 'react';
import { WeatherCard, CardsWrapper } from '../WeatherCard'
import { Button } from '../Button'

const StyledWeatherPanel = ``;

const WheatherInfo = () => {

}

export const WeatherPanel = ({ forecast }) => {
  return (
    <div> 
      <h1>Panel</h1>
      
    </div>
  )
}